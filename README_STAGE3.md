# 阶段 3: 数据工程

## 学习目标

1. 理解训练数据的格式要求
2. 掌握数据清洗和预处理技巧
3. 实现数据质量检查
4. 构建训练数据集

## 关键概念

### 训练数据格式

#### Alpaca 格式

最常用的指令微调数据格式：

```json
{
  "instruction": "任务描述或问题",
  "input": "输入内容（可选）",
  "output": "期望的输出"
}
```

**示例**：
```json
{
  "instruction": "请总结以下文本的主要内容。",
  "input": "今天学习了 LoRA 微调技术，它通过低秩分解大幅减少了可训练参数...",
  "output": "文本介绍了 LoRA 微调技术的核心优势：通过低秩分解减少参数量。"
}
```

#### ShareGPT 格式

对话式数据格式：

```json
{
  "conversations": [
    {"from": "human", "value": "什么是 LoRA？"},
    {"from": "gpt", "value": "LoRA 是一种参数高效的微调技术..."}
  ]
}
```

### 数据质量要素

#### 1. 数据清洗

- **去重**：移除重复样本
- **过滤**：移除低质量、过短、过长的样本
- **格式化**：统一格式、编码、标点

#### 2. 数据多样性

- 覆盖不同场景和任务
- 包含不同长度的样本
- 平衡不同类型的数据

#### 3. 数据平衡

- 避免某类数据过多
- 控制样本长度分布
- 平衡难易程度

### 数据处理流程

```
原始数据
  ↓
文本清洗（去除噪声、特殊字符）
  ↓
格式转换（转为 Alpaca/ShareGPT 格式）
  ↓
质量检查（长度、重复、空值）
  ↓
数据分割（训练集 90% + 验证集 10%）
  ↓
保存为 JSONL 格式
```

## 实践任务

### 任务 1: 运行数据处理脚本

```bash
python stage3_data.py
```

观察：
- 数据加载过程
- 样本提取结果
- 质量检查统计
- 生成的 JSONL 文件

### 任务 2: 检查生成的数据

```bash
# 查看训练集前 3 条
head -n 3 /app/data/processed/train.jsonl | jq .

# 统计样本数量
wc -l /app/data/processed/train.jsonl
wc -l /app/data/processed/val.jsonl
```

### 任务 3: 优化数据质量

修改 `stage3_data.py` 中的清洗逻辑：

```python
def clean_text(text: str) -> str:
    # 添加你的清洗规则
    # 例如：移除 URL、邮箱、特殊符号等
    text = re.sub(r'http[s]?://\S+', '', text)  # 移除 URL
    text = re.sub(r'\S+@\S+', '', text)         # 移除邮箱
    return text.strip()
```

### 任务 4: 自定义样本构造

修改 `extract_training_samples` 函数，构造更好的训练样本：

```python
# 示例：根据内容类型生成不同的 instruction
if '技术' in para:
    instruction = '请解释以下技术概念。'
elif '经验' in para:
    instruction = '请总结以下经验教训。'
else:
    instruction = '请分析以下内容。'
```

## 数据质量检查清单

- [ ] 样本数量充足（至少 100+ 条）
- [ ] 平均长度合理（50-500 字符）
- [ ] 无空样本
- [ ] 重复率低（<5%）
- [ ] 格式正确（Alpaca 格式）
- [ ] 训练集/验证集分割合理（9:1）

## 常见问题

**Q: 需要多少训练数据？**
A:
- 最少：100-500 条（小规模微调）
- 推荐：1000-5000 条（中等规模）
- 理想：10000+ 条（大规模微调）

**Q: 如何处理长文本？**
A:
- 截断：保留前 N 个 token
- 分段：将长文本切分为多个样本
- 摘要：提取关键信息

**Q: 数据不平衡怎么办？**
A:
- 过采样：复制少数类样本
- 欠采样：减少多数类样本
- 加权：给少数类更高的权重

## 里程碑检查点

- [ ] 理解 Alpaca 数据格式
- [ ] 掌握数据清洗技巧
- [ ] 完成数据质量检查
- [ ] 生成训练和验证数据集

## 下一步

完成本阶段后，进入 **阶段 4: 训练优化**，学习如何配置和执行微调训练。
